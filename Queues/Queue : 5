The seven elements 1, 2, 3, 4, 5, 6 and 7 are pushed onto a stack in reverse order, i.e., starting from 7.

The stack is popped five times and each element is inserted into a queue.

Two elements are deleted from the queue and pushed back onto the stack.

Now, one element is popped from the stack. The popped item is

Correct Answer : 2
